//Recursive Function
int sumDigitsRec(int x) {
	if (x < 10) { return x; } // if it's a single digit number, return the number. 
	return (x % 10) + sumDigitsRec(x / 10);
}

//Driver Function 
int sumDigits(int x) {
	if (x < 0) x = x*(-1);
	return sumDigitsRec(x);
}
